node_modules
dist
dist-ssr
.DS_Store
*.local
.vite
src-tauri/target

# local secrets
.env
.env.local

# Generated runtime assets. scripts/copy-runtime-assets.mjs writes
# public/assets/dice-box and public/dict; dice-box's own postinstall also drops
# copies at public/assets/themes and public/assets/ammo. All of it comes from
# node_modules, so none of it belongs in the repo.
public/assets/
public/dict/
